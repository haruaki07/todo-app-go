package model

// asdasdad
// should not do this on real application, save the configuration on separate config file
const (
	DBHost     = "localhost"
	DBPort     = 5432
	DBUser     = "postgres"
	DBPassword = "root"
	DBName     = "todo_app_go"
)
